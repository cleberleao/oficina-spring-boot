pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                //sh 'docker build -t oficina-spring .'
                //
            }
        }

        stage('Deploy') {
            steps {
                //sh 'docker run -d -p 8080:8080 oficina-spring --name oficina-spring'
                //
            }
        }
    }
}